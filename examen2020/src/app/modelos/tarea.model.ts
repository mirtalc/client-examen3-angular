/**
 * Hola, Salva!! Ya sé que debería haber sido una interfaz,
 * pero me estaba dando problemas y no caigo en qué ha pasado.
 * Así que al final lo he dejado como una clase normal.
 * A ver si en casa me viene qué estaba haciendo mal...
 */

export class Tarea {
  // Atributos
  id: number;
  nombre: string;
  estado: string;
}
